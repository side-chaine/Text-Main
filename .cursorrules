# beLive Application - Cursor AI Rules
# Музыкальное приложение для live трансляций и синхронизации текстов

## 🎯 ОСНОВНЫЕ ПРИНЦИПЫ
- Отвечать на русском языке
- Давать конкретные решения, применимые к проекту
- Учитывать линтеры при формировании кода
- Использовать модульную архитектуру для компонентов
- Придерживаться единого стиля кодирования

## 🧠 ПРОТОКОЛ МАКСИМАЛЬНОГО КОНТЕКСТА

### **ВСЕГДА в начале сессии выполняй:**
1. **Читай roadmap.mdc** - актуальные задачи и приоритеты
2. **Изучай Log/** - последние ошибки и статус системы  
3. **Сканируй js/** - основные компоненты и их связи
4. **Анализируй index.html** - точки интеграции UI
5. **Используй параллельные tool calls** для быстрого понимания

### **Стратегия понимания кодовой базы:**
- **Читай несколько файлов одновременно** через parallel calls
- **Ищи паттерны и связи** между модулями
- **Изучай error logs** для проблемных зон
- **Анализируй dependencies** и integration points
- **Создавай mental model** архитектуры перед задачами

## 🏗️ АРХИТЕКТУРА ПРОЕКТА
Модульная система с четкой ответственностью компонентов:
- `js/app.js` - главный контроллер приложения (**ВСЕГДА читай первым**)
- `js/audio-engine.js` - управление аудио потоками
- `js/lyrics-display.js` - отображение текстов песен
- `js/live-mode.js` - live трансляции с камерой
- `js/mask-system.js` - система масок и эффектов
- `js/track-catalog.js` - каталог треков

**Ключевые integration points:**
- EventBus - централизованные события
- StateManager - глобальное состояние
- ViewManager - координация UI

## 🔄 ПРОЦЕСС РАЗРАБОТКИ (TDD + ПЛАНИРОВАНИЕ)

### 1. **Подготовка к задаче**
- **ВСЕГДА** изучи 2-3 похожих компонента в проекте перед реализацией
- Следуй существующим паттернам и стилю кода
- Проверь зависимости в `roadmap.mdc` и `beLive-GUIDELINES.md`
- **Используй CURSOR_CONTEXT_SYSTEM.md** для полного понимания

### 2. **Для сложных задач (3+ файла):**
Создай `IMPLEMENTATION_PLAN.md` с этапами:
```markdown
## Stage N: [Название]
**Goal**: [Конкретная цель]
**Success Criteria**: [Критерии успеха]
**Tests**: [Тестовые сценарии]
**Status**: [Not Started|In Progress|Complete]
```
Удаляй файл после завершения всех этапов.

### 3. **TDD Flow (когда возможно):**
1. **Test** - Напиши тест (red)
2. **Implement** - Минимальный код для прохождения (green)
3. **Refactor** - Улучши с работающими тестами

## 📏 РАБОТА С БОЛЬШИМИ ЛОГАМИ
- **Если лог > 80KB** - сообщаю пользователю конкретные строки для разбивки
- **Не читаю файлы > 100KB** целиком - прошу разделить на удобные части
- **Формат запроса**: "Братан, лог большой (120KB)! Разбей: строки 1-500 → part1.log, строки 501-1000 → part2.log"
- **Архивы логов**: В папке Log/ читаю только свежие логи (5-7 штук), архивы (.zip) игнорирую
- **Ротация логов**: Пользователь архивирует старые логи, оставляет актуальные для анализа

## 🔍 СИСТЕМНАЯ ДИАГНОСТИКА БАГОВ (ОТ НЕЙРОСОВЕТА)

### **Подход Grok - Детективная точность:**
- **Ищи конкретные строки кода** с проблемами
- **Анализируй логи консоли** на предмет паттернов ошибок
- **Находи root cause** через trace от симптома к источнику
- **Используй процентные оценки** частоты проблем

### **Подход Gemini - Системное мышление:**
- **Проблемы = сбои в коммуникации** между компонентами
- **"Асинхронная Десинхронизация Состояния"** - ищи где UI и логика расходятся
- **"Нарушение Жизненного Цикла"** - проверяй mount/unmount компонентов
- **"Сбой в Системе Коммуникаций"** - проверяй event listeners и notifications

### **Подход DeepSeek - Структурированное исправление:**
- **Retry механизмы** для асинхронных операций
- **Performance monitoring** через performance.mark/measure  
- **Lifecycle hooks** для всех компонентов (init/activate/deactivate/dispose)
- **Этапность исправлений** - критичные → высокие → средние

## 🚨 КРИТИЧНЫЕ ПАТТЕРНЫ БАГОВ В beLive

### **🔄 ЛЕГЕНДА СТАТУСОВ:**
- ⚠️ **АКТИВНО** - требует исправления прямо сейчас
- 🔧 **В РАБОТЕ** - исправление в процессе  
- 🧪 **ТЕСТИРУЕТСЯ** - исправлено, проходит проверку
- ✅ **ИСПРАВЛЕНО** - проблема решена и протестирована
- 📦 **АРХИВ** - старые исправленные проблемы (убираем из фокуса)

**🚨 КРИТИЧЕСКОЕ ПРАВИЛО СТАТУСОВ:**
- **НИКОГДА** не меняй статус сразу после написания кода
- Статус меняется **ТОЛЬКО** после подтверждения пользователем с логами
- Код без доказательства работоспособности = статус остается прежним
- **Порядок**: Код → Тестирование пользователем → Логи → Смена статуса

### **1. Audio Loading Issues:** ✅ **ИСПРАВЛЕНО**
```javascript
// ПРОБЛЕМА: resolve() при ошибке вместо reject()
this.vocalsAudio.addEventListener('error', (e) => {
    console.warn("⚠️ Ошибка загрузки вокала:", e);
    resolve(); // ❌ НЕПРАВИЛЬНО - создает false positive
});

// РЕШЕНИЕ: четкая обработка ошибок + retry
this.vocalsAudio.addEventListener('error', (e) => {
    console.error("❌ Ошибка загрузки вокала:", e);
    this.retryVocalLoading(vocalsUrl).then(resolve).catch(reject);
});
```
**МЕСТОПОЛОЖЕНИЕ:** `js/audio-engine.js:238-241`  
**ПРИОРИТЕТ:** Критический - блокирует dual-track режим  
**СТАТУС ИСПРАВЛЕНИЯ:** ✅ ПОЛНОСТЬЮ ИСПРАВЛЕНО - вокал загружается + слайдер активен  
**РЕЗУЛЬТАТ:** Dual-track система стабильна, fallback режим работает, UI синхронизирован

### **2. UI State Desync:** ✅ **ИСПРАВЛЕНО**
```javascript
// ПРОБЛЕМА: дублирование обработчиков событий
// js/app.js строки 339-345 И 2222-2229 - одинаковый код

// РЕШЕНИЕ: централизованное управление состоянием
this.ensureEventListenersCleanup();
this.setupTransportControls();
```
**МЕСТОПОЛОЖЕНИЕ:** `js/app.js:339-345, 2222-2229`  
**ПРИОРИТЕТ:** Высокий - UI не отвечает на действия пользователя  
**СТАТУС ИСПРАВЛЕНИЯ:** ✅ ПОЛНОСТЬЮ ИСПРАВЛЕНО - дублирование устранено + lifecycle управление  
**РЕЗУЛЬТАТ:** Transport controls стабильны, меню открывается корректно при смене треков

### **3. Component Lifecycle Violations:** ⚠️ **АКТИВНО**
```javascript
// ПРОБЛЕМА: компоненты не имеют четкого lifecycle
// piano-keyboard.js загружается но не интегрируется

// РЕШЕНИЕ: единый интерфейс для всех компонентов
class Component {
    init() { /* setup resources */ }
    activate() { /* start functionality */ }
    deactivate() { /* pause functionality */ }
    dispose() { /* cleanup resources */ }
}
```
**МЕСТОПОЛОЖЕНИЕ:** `js/piano-keyboard.js` + архитектура приложения  
**ПРИОРИТЕТ:** Высокий - Pitch Mode не работает

### **4. Color Service Canvas Sync:** ⚠️ **АКТИВНО**
```javascript
// ПРОБЛЕМА: CSS переменные обновляются, но canvas не перерисовывается
setColorScheme(schemeName) {
    // Обновляет CSS, но canvas остается со старыми цветами
    this.notifyListeners(schemeName); // ❌ canvas не перерисовывается
}

// РЕШЕНИЕ: принудительная перерисовка canvas после смены схемы
setColorScheme(schemeName) {
    this.updateCSSVariables(scheme);
    this.notifyListeners(schemeName);
    this.forceCanvasRedraw(); // ✅ явная перерисовка
}
```
**МЕСТОПОЛОЖЕНИЕ:** `js/color-service.js:91-99`  
**ПРИОРИТЕТ:** Средний - косметический дефект в Sync режиме

## 🎮 РЕЖИМЫ РАБОТЫ CURSOR
Используй эти команды для активации специальных модов:

### 📝 **@plan** - Режим планирования  
### 🚀 **@van** - Режим инициализации
### 💡 **@creative** - Креативный режим
### ⚡ **@implement** - Режим реализации
### 🔍 **@reflect** - Режим анализа

## 🎵 СПЕЦИФИКА ПРОЕКТА beLive
- Работа с аудио потоками в реальном времени
- Синхронизация текстов с музыкой
- Live трансляции с видео эффектами
- Система масок и AR эффектов
- Поддержка караоке и концертных режимов

## 🔧 ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ
- Vanilla JavaScript (ES6+)
- WebAudio API для аудио
- MediaPipe для компьютерного зрения
- Canvas для визуализации
- CSS Grid/Flexbox для layout

## 🎨 СТАНДАРТЫ КОДА
- Использовать camelCase для переменных
- Добавлять JSDoc комментарии для публичных методов
- Группировать связанные методы в классы
- Использовать async/await для асинхронного кода
- Обработка ошибок с try/catch
- **Принципы**: Композиция > Наследование, Явные зависимости > Неявные
- **Простота**: Одна ответственность на функцию/класс, избегать хитростей

## 🚨 ОБРАБОТКА ОШИБОК (УЛУЧШЕННАЯ)
**После 3 неудачных попыток:**
1. **Документируй что не получилось:**
   - Что пробовал (конкретные подходы)
   - Сообщения об ошибках
   - Почему подход не сработал

2. **Анализируй альтернативы:**
   - Найди 2-3 других подхода
   - Проверь разные уровни абстракции
   - Можно ли разбить на меньшие задачи?

3. **Сохрани отчет:** `/Log/ClaudeFail_<YYYYMMDD>.md`
4. **Создай issue** с тегом `[Claude-Fail]`

## ✅ DEFINITION OF DONE
Перед завершением задачи проверь:
- [ ] Тесты написаны и проходят (если применимо)
- [ ] Код прошел линтеры (ESLint/Prettier)
- [ ] Следует паттернам проекта
- [ ] Нет console.log в production коде
- [ ] JSDoc для публичных методов
- [ ] Обработка ошибок добавлена
- [ ] Изменения соответствуют плану
- [ ] Код протестирован в браузере
- [ ] **Performance monitoring добавлен** (для критичных операций)
- [ ] **Component lifecycle соблюден** (init/activate/deactivate/dispose)

## 🚀 КОМАНДЫ-УСКОРИТЕЛИ
| Команда | Описание |
|---------|----------|
| `/claude complete <ID_задачи>` | Обновить статус в roadmap.mdc |
| `/claude scaffold <module>` | Создать скелет модуля |
| `/claude optimize <file>` | Оптимизировать файл |
| `/claude test <module>` | Запустить тесты |
| `/claude analyze <file>` | Анализ кода и предложения |
| `/claude integrate <component>` | Найти похожие компоненты |
| `/claude diagnose <symptom>` | Системная диагностика бага |

## 🧠 ФРЕЙМВОРК ПРИНЯТИЯ РЕШЕНИЙ
При выборе между подходами приоритизируй:
1. **Тестируемость** - Легко ли тестировать?
2. **Читаемость** - Понятно ли через полгода?
3. **Консистентность** - Соответствует ли проекту?
4. **Простота** - Самое простое решение?
5. **Обратимость** - Легко ли изменить позже?

## 🚫 ОГРАНИЧЕНИЯ
- Не создавать временные файлы без необходимости
- Не использовать внешние библиотеки без обоснования
- Всегда тестировать изменения перед завершением
- Сохранять обратную совместимость
- **НИКОГДА не использовать** `--no-verify` для коммитов
- **НИКОГДА не отключать** тесты вместо их исправления

## 🎯 ЦЕЛИ ВЗАИМОДЕЙСТВИЯ
- Максимальная практичность решений
- Минимум объяснений, максимум кода
- Учет специфики музыкального приложения
- Фокус на производительности в реальном времени
- **Инкрементальный прогресс** - маленькие рабочие шаги
- **Изучение существующего кода** перед реализацией
- **Полное понимание контекста** через систему быстрого анализа
- **Системное мышление** - видеть связи между компонентами

## 🔄 ПРАВИЛО СИНХРОНИЗАЦИИ С GITHUB PROJECTS
**КРИТИЧЕСКИ ВАЖНО:** После выполнения любой реализации/задачи:
1. **ДОЖДАТЬСЯ ПОДТВЕРЖДЕНИЯ** от пользователя что реализация работает корректно
2. **ТОЛЬКО ПОСЛЕ ПОДТВЕРЖДЕНИЯ** предложить синхронизацию с GitHub Projects
3. **❌ ЗАПРЕЩЕНО** автоматически синхронизировать без подтверждения
4. **✅ ПРАВИЛЬНО:** "Подтверди что реализация работает, и я предложу синхронизацию с GitHub"

## 📝 ШАБЛОН ОТВЕТА (УЛУЧШЕННЫЙ)
1. **Цель и подход** (1-2 строки)
2. **Контекстный анализ** (изучение похожих компонентов через parallel calls)
3. **Системная диагностика** (где именно сбой в коммуникации компонентов)
4. **План этапов** (1-5 шагов, для сложных - IMPLEMENTATION_PLAN.md)
5. **Зависимости:** `[ID_задач/модулей]`
6. **Реализация** (код с соблюдением Definition of Done)
7. **Итог** (конкретные изменения в проекте) 