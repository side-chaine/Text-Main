# Дорожная Карта Оптимизации Приложения beLive

Это правило содержит стратегические направления для будущей оптимизации приложения, предложенные AI. Эти пункты являются целями для улучшения производительности, масштабируемости и удобства поддержки кода.

## 🎯 Направления Оптимизации:

1.  **Оптимизация Сборки и Загрузки Кода**:
    *   **Code-splitting (динамические импорты)**: Разделение больших модулей (RTF-парсер, LRC-парсер, WaveformEditor, AR-модуль) на отдельные точки входа для динамической загрузки по необходимости.
    *   **Tree-shaking и удаление "мертвого" кода**: Анализ и удаление неиспользуемого кода и библиотек.

2.  **Работа с Текстом и RTF**:
    *   **Замена собственного RTF-парсера**: Рассмотрение использования сторонних библиотек, таких как `rtf.js` или WebAssembly-порт `UnRTF`, для более надежного и чистого парсинга.
    *   **Парсинг в Web Worker**: Перенос ресурсоемких операций парсинга RTF и LRC в Web Worker для предотвращения блокировки основного потока UI.

3.  **Оптимизация UI-рендеринга**:
    *   **Виртуализация списка лирики**: Рендеринг только видимых строк текста для больших файлов лирики с целью улучшения производительности скролла.
    *   **Throttle/Debounce для событий**: Применение `throttle` или `debounce` для обработчиков событий скролла, ресайза, drag'n'drop для снижения нагрузки.
    *   **OffscreenCanvas и requestAnimationFrame**: Использование OffscreenCanvas для рендеринга волновой формы в воркере и обновления через `requestAnimationFrame` для плавной визуализации.

4.  **Оптимизация Аудио-движка (AudioEngine)**:
    *   **Рефакторинг AudioEngine**: Разделение загрузки/декодирования аудио от управления BPM для повышения модульности и ясности.
    *   **Кэширование декодированных буферов**: Использование `WeakMap` или `IndexedDB` для хранения `AudioBuffer`, чтобы избежать повторного декодирования файлов.

5.  **Стилевые и Структурные Правки**:
    *   **CSS-модули / CSS-in-JS**: Локализация стилей компонентов для избежания конфликтов и лучшей организации.
    *   **Единая система темизации**: Вынесение стилей и цветов в единый JSON-конфиг для легкого управления темами.
    *   **Переход на TypeScript (опционально)**: Внедрение типизации для критически важных модулей для улучшения надежности и упрощения рефакторинга в долгосрочной перспективе.

6.  **Мониторинг Производительности**:
    *   **Web Vitals и Lighthouse**: Настройка CI/CD для автоматической проверки метрик производительности.
    *   **Профилирование**: Использование встроенных инструментов браузера для выявления узких мест в производительности.

## ⚠️ Важное Примечание:
Данное правило служит ориентиром. Реализация этих оптимизаций будет производиться поэтапно, исходя из приоритетов и текущих задач проекта. Не все пункты требуют немедленной реализации. 